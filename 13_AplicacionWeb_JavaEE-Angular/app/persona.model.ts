export class Persona {
  /*Aqui este contructor tiene estos 2 atributos que utilizaremos
   el export es para que opodamos utilizar esta clase en otras clases del proyecto  */
  constructor(public idPersona: number, public nombre: string ){ }
}
